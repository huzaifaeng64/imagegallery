<<<<<<< HEAD

  <div class="row">
    <div class="col-md-12" id="show-attachment">
      
    </div>
  </div>
      <hr>
  <div class="row">
      <%= render 'all_attachments', locals: { attachments: @attachments} %>
  </div>




<% if false %>
<div>
  <div class="row">
    <% @attachments.each do |attachment| %>
      <div class="image-text m-2">
        <%= image_tag(attachment.image.thumb.url) %>
        <div class="centered">
          <%= link_to "Show" , attachment, class: "btn-image" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
<% end %>
=======
<h1> All Images </h1>


<% @users.each do |user| %>
<% if user.attachments.any? %><table><tr><td>
	<p> <strong> User : </strong> <%= user.username %> </p>
		<% user.attachments.each do |attachment| %>
			<%= link_to( image_tag(attachment.image.thumb.url, alt: "image"), attachment ) if attachment.image? %>	
		<% end %>	<p>
		<%= link_to " Show Gallery", gallery_path(user.attachments.ids) %>
	</p>
		<hr>
<% end %>
<% end %>



>>>>>>> reaction-functionality
