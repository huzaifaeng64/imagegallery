<<<<<<< HEAD
<div class="text-center">
  <h1>Create Gallery</h1>
  <p><strong> User Name:</strong> <%= current_user.username if current_user.username? %> </p>
  <p> Total Attachments: <%= current_user.attachments.count %> </p>
   <p>
    <% if current_user.attachments.any? %>
        <%= link_to "Show Gallery", gallery_path(current_user.attachments.ids), class: 'btn btn-info'%>
      <% end %>
  </p>
</div>
<hr>
<div class="row">
  <div class="col-md-6">
    <h2> Your Attachments </h2>
    <hr>

    <div class="row text-center">
      <div class="col-md-2">
        <Strong> Title </Strong>
      </div>
      <div class="col-md-2">
        <Strong> Amount </Strong>
      </div>
      <div class="col-md-3">
        <Strong> Image </Strong>
      </div>
    </div>
    <%= render current_user.attachments %>

  </div>

  <div class="col-md-6">
    <div class=" border border-dark p-4 nested-form">
      <h2> Add New Attachments </h2>
      <hr>
      <%= nested_form_for current_user do |f| %>
        <%= f.fields_for :attachments, Attachment.new do |f| %>
          <div>
            <%= render "attachments/create_attachment", f: f %>
            <hr>
          </div>
        <% end %>
        <div>
          <%= f.link_to_add "Add new Attachment", :attachments, class: "btn btn-info float-right" %>
        </div>
        <div>
          <%= f.submit "Submit" , class: "btn btn-primary"%>
        </div>
      <% end %>
    </div>
  </div>
  <hr>
</div>



=======

<h1> My Gallery </h1>

<p>
  <strong> User Name : </strong> <%= current_user.username %>
</p>

<% if current_user.attachments.any? %>
  <p>
    <%= link_to "Show Gallery", gallery_path(@attachments.first.id) %>
  </p>
<% end %>
<hr>

<h2> All Images </h2>

<%= link_to 'Add New Attachment', new_attachment_path %>

<P>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Amount</th>
      <th>Image</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @attachments.each do |attachment| %>
        <tr>
  		    <td>
			     <%= attachment.title %>
		      </td>
          <td> <%= attachment.amount %>
          </td>
		     <td>
            <%= link_to( image_tag(attachment.image.small_thumb.url, alt: "image"), attachment.image.url, target: " ") if attachment.image? %>
		     </td>
         <td>
          <%= link_to 'Show', attachment %> |
          <%= link_to 'Edit', edit_attachment_path(attachment) %> | 
          <%= link_to 'Destroy', attachment, method: :delete, data: { confirm: 'Are you sure?' } %>
          </td>
	      </tr>
    <% end %>
  </tbody>
</table>

</P>
>>>>>>> reaction-functionality
